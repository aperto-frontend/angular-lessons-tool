import { ParentPercentWidthDirective } from './parent-percent-width.directive';

describe('ParentPercentWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new ParentPercentWidthDirective();
    expect(directive).toBeTruthy();
  });
});
